CONFIG = {
    'gal_type': 'exp',
    'psf_type': 'gauss',
    'shear_scene': True,
    'n_coadd': 30,
    'scale': 0.263,
}

# turn on/off metadetect with matching on different shear populations
match = True
# for negative shear, invert (or not) the sign of input g1ex
invert_ex = True
